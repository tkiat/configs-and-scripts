source ~/.bashrc.shared

. ~/.alias-modules/nix
. ~/.alias-modules/nixos
. ~/.alias-modules/systemctl

alias 2showoff='neofetch && echo "ls /run/current-system/firmware" && ls /run/current-system/firmware'
